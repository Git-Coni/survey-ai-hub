{
  "name": "backend",
  "version": "1.0.0",
  "main": "dist/server.js",
  "scripts": {
    "start": "cross-env NODE_ENV=local node dist/server.js",
    "dev": "cross-env NODE_ENV=local ts-node src/server.ts",
    "dev:dev": "cross-env NODE_ENV=dev ts-node src/server.ts",
    "dev:prd": "cross-env NODE_ENV=prd ts-node src/server.ts",
    "build": "cross-env NODE_ENV=local tsc",
    "build:dev": "cross-env NODE_ENV=dev tsc",
    "build:prd": "cross-env NODE_ENV=prd tsc",
    "start:dev": "cross-env NODE_ENV=dev node dist/server.js",
    "start:prd": "cross-env NODE_ENV=prd node dist/server.js",
    "migrate": "node database/migrate.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@google/generative-ai": "^0.11.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "mysql2": "^3.1.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.8.3",
    "winston": "^3.17.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/node": "^24.0.13",
    "cross-env": "^7.0.3"
  }
}
